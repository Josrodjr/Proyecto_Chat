// librerias que vamos a usar
// structs
#include <sys/types.h> 
// sockets y puertos
#include <sys/socket.h>
// red
#include <netdb.h>
// basicas
#include <stdio.h>
#include <string.h>

int main() {
    // structs que tienen el socket address y server address
    struct sockaddr_in server_address;
    // file descriptors
    int list, comm;

}